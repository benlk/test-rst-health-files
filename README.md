This repo is testing an assumption:

- That GitHub supports sourcing community health files from both `.github/` and `docs/` in the same repo
- That GitHub supports making community health files in RST
